# SQL 기초 학습 내역

## SQL이란?
SQL은 Structured Query Language, 관계형 데이터베이스(RDBMS)에 저장된 데이터를 관리하고 소통하기 위해서 사용하는 **데이터베이스 표준 언어**이다. 우리가 컴퓨터와 대화하기 위해서 파이썬/자바를 하용하듯, 데이터베이스와 대화하기 위한, 구조화된 쿼리(DB에 요청하는 명령)를 다루는 언어를 말한다. '영어 문장처럼 쉽게 데이터베이스를 다뤄보자'는 아이디어로 시작한 것이 지금까지 살아남아서 데이터 세계의 공용어로 군림하고 있는 것이다.



SQL은 1970년에 Edgar F. Codd박사가 1970년데 '관계형 데이터 모델'이론을 발표한 뒤 4년만에, IMB의 Donald D. Chamberlin과 Raymond F. Boyce가 그 이론을 바탕으로 데이터베이스를 제어하기 위한 언어인 SEQUEL(Structured English Query Language)을 만들었고, 이후 상표권 문제로 스펠링이 우리가 익히 아는 'SQL'로 바뀌었지만, 발음은 여전히 [시퀄]로 읽는다.


## SQL의 구분
SQL은 DDL, DML, DQL, DCL, TCL 총 다섯개의 명령어 그룹으로 분류된다.

### DDL (Data Definition Lanaguage)
우선 DDL부터 보자. DDL은 데이터(Data) 정의(Definition) 명령어(Language)로서, 데이터베이스의 뼈대/구조를 생성, 변경, 삭제하는 명령어 그룹이다. 쉽게 말하자면 데이터가 들어갈 테이블 구조 자체를 만들고, 뜯어고치고, 부수는 역할을 하는 명령어다. 주요 명령어로는
- 테이블이나 데이터베이스를 처음부터 만드는 `CREATE`
- 새로운 열을 추가하거나 열을 삭제하는 등, 기존에 있던 테이블의 구조를 변경하는 `ALTER`
- 테이블/데이터베이스를 통째로 삭제하는 `DROP`
등이 있다.


### DML (Data Manipulation Language)
DML은 Table "안"의 각각 데이터를 조작하는, 데이터(Data) 조작(Manipulation) 명령어(Language)이다. 만들어진 데이터베이스 안에 자료들을 입력하고, 바꾸고, 버리는 작업을 할 수 있다. 주요 명령어로는,
- 테이블에 새로운 데이터를 삽입하는 `INSERT`
- 기존 데이터를 수정하는`UPDATE`
- 특정 데이터를 선택해서 삭제하는`DELETE`
등이 있다.


### DQL (Data Query Language)
DQL은 데이터베이스 안에 저장된 데이터를(Data) 조회/검색(Query)하는 명령어(Language)이다. 엄밀히 따지면 DQL은 DML의 하위 분류로 본다고 하긴 하는데, 이전에 Google Cloud Study Jam을 할 때, SQL/BigQuery 관련 실습 레슨에서는 `SELECT` 이외의 명령어를 입력해 본 적이 많이 없던 것으로 기억한다. AI 시대가 다가온 만큼 특정한 조건을 만족하는 데이터를 "읽어오는" 것이 아주 중요해진 것이다. 주요 명령어로는,
- DB에서 원하는 조건의 데이터를 선택해서 불러오는 `SELECT`
가 대표적이다.

### DCL (Data Control Language)
DCL은 데이터(Data)를 제어(Control)하는 명령어(Language)이다. 작년에 처음으로 PostgreSQL을 설치하자마자 대표 계정 비밀번호를 만들고 로그인을 하는 과정을 거치면서 "와, 나밖에 건드릴 사람이 없는 상황에서도 관리가 빡세구나" 생각을 했었지만, 본디 DB라는 것이- 많은 사람들이 접근하고 귀중한 자료들이 저장되어있는 DB는 접근 권한을 제한하고, 인가된 사람들만 접근할 수 있게 권한의 선택적 부여가 필요한 것이다. 주요 명령어로는,
- 특정 사용자에게 데이터베이스 접근 및 작업 권한을 부여하는 `GRANT`
- 사용자에게 부여했던 권한을 회수하는 `REVOKE`
등이 대표적이다.

### TCL (Data/Transaction Control Language)
TCL은 데이터의 트랜잭션(Transaction) 제어(Control) 명령어(Language)이다. 상술한 DML(`INSERT`, `UPDATE`, `DELETE`등 테이블 안의 데이터를 작성 및 수정하는 명령어 뭉치들)로 데이터를 조작한 뒤에, 그 결과를 실제 데이터베이스에 최종 반영할지, 아님 취소하고 이전 상태로 되돌릴(롤백)지 결정하는 명령어이다. 마치 Git 등지에 commit하고 rebase하고 reset 하는 느낌이 많이 들었다. 주요 명령어로는,

- 방금 한 작업들을 디스크에 영구 저장하는 명령어인 `COMMIT` (commit을 해야 다른 작업자들에게도 반영된 결과물이 보인다)
- 바로 직전의 Commit 직후 상태로 작업을 전부 취소하고 되돌리는 `ROLLBACK`
- Query가 길 때, 중간에 체크포인트를 만들어 두고, 에러가 나면 맨 처음이 아니라 그 특정 지점으로 되돌릴 수 있게 만들어놓은 `SAVEPOINT`
등이 대표적이다.


## CRUD란?
Create, Read, Update, Delete

## SQL의 6대 명령어 (1/2)

### SELECT
- column(열)을 선택하는 명령어

### FROM
- table(표)를 선택하는 명령어

### WHERE
- rows(행)을 선택하는 명령어

#### 그러므로, 이 세가지를 합쳐보자면 기본적인 SQL 명령어 형태가 등장한다.

```SQL
SELECT 컬럼명1, 컬럼명2, ...
FROM 테이블명
[WHERE 조건식];
```
말인즉슨 [`테이블명` 테이블의 `컬럼명 1, 2, ...` 컬럼에서 `조건식`을 만족하는 행을 가져오라]는 요청이 되는 것이다.

#### 재미있는 것: 우리는 코드를 적을 때 `SELECT` -> `FROM` -> `WHERE` 순서로 적지만, 컴퓨터는 `FROM`(테이블을 확인) -> `WHERE`(조건 필터랑) -> `SELECT`(결과 열 출력) 순서로 실행한다는 것을 기억해 두면, 나중에도 복잡한 쿼리를 짤 때/트러블슈팅을 할 때 도움이 된다.

### 위의 조건문을 조금 더 연습해보기 위해서 다음과 같은 DB 테이블, *employees* 가 있다고 가정해 보자.
[employees]
| id (사원번호) | name (이름) | department (부서) | salary (급여) |
| :--- | :--- | :--- | :--- |
| 1 | 김철수 | 마케팅 | 3000 |
| 2 | 이영희 | 개발 | 4500 |
| 3 | 박민수 | 개발 | 5000 |
| 4 | 최지우 | 인사 | 3500 |

여기서 요청을 해 보자.
1. 직원 테이블의 정보를 모두 보여주세요!
```SQL
SELECT * FROM employees;
```

2. 직원들의 '이름' 과 '부서'만 골라서 보여주세요!
```SQL
SELECT name, department
from employees;
```

3. 부서가 '개발'인 직원들의 이름과 급여를 보여주세요!
```SQL
SELECT name, salary
FROM employees
WHERE department = '개발';
```

4. 급여가 4000 이상인 모든 사원들의 정보를 보여주세요!
```SQL
SELECT *
FROM employees
WHERE salary >= 4000;
```

```SQL

```



## SQL의 6대 명령어 (2/2)

### GROUP BY
- 전체 데이터들을 특정 기준에 따라 그룹으로 모아서 합계, 평균, 개수 등 특정한 방식으로 그룹화하는 명령어.

예시: 부서별로 급여 평균이 얼마인가요!
```SQL
SELECT department, AVG(salary)
FROM employees
GROUP BY department;
```
#### `GROUP BY department`를 적으면, 컴퓨터는 '마케팅팀' '개발팀' '인사팀' 등등의 상자를 만들고, 직원들을 알맞은 상자에 집어넣은 뒤에, AVG(평균)을 계산한다.

### HAVING
- 그룹된 데이터를 필터링하는 명령어. WHERE 절이 개별 행을 필터링한다면, HAVING 절은 GROUP BY 로 묶인 그룹 결과를 필터링함. 반드시 GROUP BY 뒤에 위치해야 한다.
예시: 부서별 평균 급여를 구하는데, 평균 급여가 4000 이상인 부서만 보여주세요!

```SQL
SELECT department, AVG(salary)
FROM employees
GROUP BY department
HAVING AVG(salary) >= 4000;
```
#### WHERE Vs. HAVING:
- `WHERE`: 집계 함수 (AVG, SUM, COUNT 등)를 사용할 수 없음. (예: `WHERE AVG(salary) >= 4000;` 같이 적고 실행하면 에러가 난다!)
- `HAVING`: 집계 함수를 사용한 조건식에 사용함


### ORDER BY
- 데이터를 줄세우는 기준을 말해주는 명령어. SQL 문장의 가장 마지막에 위치한다. 예시를 보면:
    - 오름차순: `ASC` (작은값-> 큰값으로, / 가나다 순) -_default 값이다_
    - 내림차순: `DESC` (큰 값 -> 작은앖으로 / 하파타 순)

예시: 모든 직원의 정보를 **급여가 *높은* 사람부터(*내림차순*)** 보여주세요!

```SQL
SELECT *
FROM employees
ORDER BY salary DESC;
```


### 이외의 명령어

### COUNT
- 해당 쿼리를 만족하는 행(데이터)의 갯수를 반환한다.
예시: 전체 직원은 총 몇 명인가요?
```SQL
SELECT COUNT(*) FROM employees;
```


### LIMIT
- LIMIT 5와 같이 상위 5개 등 출력하는 자료의 갯수를 제한한다. 대개 ORDER BY와 같이 쓰인다.
예시: 우리 회사에서 급여를 많이 받는 순으로 **상위 *2명***만 보여주세요!
```SQL
SELECT *
FROM employees
ORDER BY salary DESC
LIMIT 2;
```

### DISTINCT
- 출력될 시에 중복되는 데이터를 없애줌 
- `SELECT DISTINCT` 등으로 활용
예시: 우리 회사에 존재하는 부서들의 종류를 보고 싶어요.
```SQL
SELECT DISTINCT department FROM employees;
```
#### 테이블의 정규화가 잘 될수록 DISTINCT를 쓰는 횟수가 줄어들 것이다!


```SQL
SELECT   -- (5) 무엇을 조회할 것인가? (DISTINCT, COUNT 등 포함)
FROM     -- (1) 어느 테이블에서 가져올 것인가?
WHERE    -- (2) 개별 행을 어떤 조건으로 거를 것인가?
GROUP BY -- (3) 어떤 기준으로 그룹을 묶을 것인가?
HAVING   -- (4) 묶인 그룹을 어떤 조건으로 거를 것인가?
ORDER BY -- (6) 결과를 어떻게 정렬할 것인가? (ASC/DESC)
LIMIT    -- (7) 최종 몇 개만 화면에 보여줄 것인가?
```

## 분산된 테이블을 합쳐서 보는 JOIN
- 분산되어 있는 여러 테이블을 하나로 합쳐서 보고 싶을 때 JOIN을 활용한다.
- 정규화 등으로 많이 쪼개놓은 테이블들을 다시 조립해서 결과를 볼 수 있다.
- 기본형은 INNER JOIN(교집합) 형태이지만, 가장 많이 쓰이는 것은 LEFT JOIN이다.

역시 테이블을 2 개 보자.

[직원 테이블]
| 사원 ID  | 이름 | 부서 ID |
|:---|:---|:---|
|1|김철수|D01|
|2|이영희|D01|
|3|최지우|NULL|


[부서 테이블]
|부서 ID|부서명|
|:---|:---|
|D01|개발팀|
|D02|인사팀|

### INNER JOIN (교집합)
- 두 테이블 모두에 공통으로 존재하는 데이터만 합쳐서 보여준ㄷ. 양쪽 어느 한 곳이라도 데이터가 없다면, 결과에서 제외될 것이다.

예시: 부서 배정을 완료한 직원들과 그 부서의 이름을 함께 보여주세요!
```SQL
SELECT E.이름, D.부서명
FROM 직원 E
INNER JOIN 부서 D ON E.부서ID = D.부서ID;
```

결과:
|이름|부서명|
|:---|:---|
|김철수|개발팀|
|이영희|개발팀|
- 교집합 (그 조건: E.부서ID = D.부서ID)만 표시하니까 부서가 없는 최지우, 직원이 없는 인사팀은 나오지 않을 것이다. 

### 여기서 잠시! 직원 E? D.부서명?
#### Table Alias(테이블 별칭): `FROM 부서 (AS) D` 형태
- 부서 D: 부서 테이블을 D로 부른다 
- 직원 E: 직원 테이블을 E로 부른다

### LEFT [OUTER] JOIN: (왼쪽 기준으로 컬럼 매칭)
- 왼쪽(먼저 쓴) 테이블의 모든 데이터를 가져오고, 오른쪽 테이블에서는 매칭되는 데이터만 가져온다. 
- 오른 쪽 테이블에 매칭되는 값이 없다면 NULL로 표시된다.
- 실무에서 가장 많이 쓰이는 JOIN이기도 하다. 

예시: 우리 회사 모든 직원의 목록을 뽑아주시고, 부서가 있다면 부서명도 옆에 붙여주세요!
```SQL
SELECT E.이름, D.부서명
FROM 직원 E  -- 왼쪽(기준)
LEFT JOIN 부서 D ON E.부서ID = D.부서ID;
```
결과:
|이름|부서명|
|:---|:---|
|김철수|개발팀|
|이영희|개발팀|
|최지우|NULL|



### RIGHT [OUTER] JOIN (오른쪽 기준 컬럼 매칭)
- LEFT JOIN의 반대로, 오른쪽(나중에 쓴) 테이블의 모든 데이터를 가져오고, 왼쪽 테이블에서 매칭되는 값을 가져옴.
- 매칭되는 행이 없으면 NULL로 채워짐

예시: 회사에 존재하는 ***모든* 부서**의 목록을 뽑아주고, 각 부서에 소속된 직원이 있다면 이름도 같이 보여주세요!
```SQL
SELECT E.이름, D.부서명
FROM 직원 E  
RIGHT JOIN 부서 D ON E.부서ID = D.부서ID; -- 오른쪽(기준)
```

|이름|부서명|
|:---|:---|
|김철수|개발팀|
|이영희|개발팀|
|NULL|인사팀|
- 아직 부서가 정해지지 않은 최지우는 제외되었다.

#### 사실 RIGHT JOIN은 잘 쓰지 않는다!
- LEFT JOIN으로 통일해서 쓰는 것이 업계의 디폴트처럼 되기도 했고,
- 사람의 시선은 왼쪽에서 오른쪽으로 흐르기 때문에
- 실무에서는 대개 LEFT JOIN 하나로 통일해서 쓰는 편이다!



### 참고한 자료들


[코딩애플_1. 개발시 데이터베이스 선택 가이드](https://www.youtube.com/watch?v=ZVuHZ2Fjkl4)

[코딩애플_2.index 설명](https://www.youtube.com/watch?v=iNvYsGKelYs&t=14s)

[코딩애플_3.데이터 정규화](https://www.youtube.com/watch?v=Y1FbowQRcmI)

[코딩애플_4.SQL Injection](https://www.youtube.com/watch?v=FoZ2cucLiDs)

[노마드코더_1.DB 고민 끝내드림](https://www.youtube.com/watch?v=ocZid4g4UpY)

[노마드코더_2. SQL 5분설명](https://www.youtube.com/watch?v=z9chRlD1tec)

[노마드코더_3. SQL Vs. NoSQL](https://www.youtube.com/watch?v=Q_9cFgzZr8Q)

[노마드코더_4. SQLite 소개](https://www.youtube.com/watch?v=ax-Q1iedXJA)



이외에도 Maven Analytics의[Learn 12 Basic SQL Concepts in 15 Minutes (project files included!)](https://www.youtube.com/watch?v=_vxobA36UN4) 영상을 보면서 공부를 진행했으며,

[해당 영상의 페이지에 있는 샘플 데이터](https://www.youtube.com/redirect?event=video_description&redir_token=QUFFLUhqblkxRWJyTkpEMTFiczVpZE9SQTBKNXR5NWdwUXxBQ3Jtc0tuRnE4UmlYcWNKZ3BGRjJuZDB0N3EwQmNzV3F3c1BIYnYwbDdqNHphSjhva2hsQUh3c3NCanMtRUFLd2N5UGZMX1A3UlR3dEw1ajdvQVhoQ05xUUxwbFZOMzBUWTcyR2c2X2Z6bWdRRmppTjdYY3ZaSQ&q=https%3A%2F%2Fmaven-datasets.s3.amazonaws.com%2FYouTube%2BDownloads%2Fmaven-student-grades-db.zip&v=_vxobA36UN4)를 활용하여 기본적인 연습을 해 볼수도 있다.






